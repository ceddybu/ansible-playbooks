---
# Global
cpu_cores: "{{ ansible_processor_vcpus }}"

# http://www.php.net/manual/en/timezones.america.php#114172
php_tz: America/Chicago
os_tz: US/Central

# MySQL 5.5 Database: mariadb, percona, mysql55-ius
mysql_host: db01.raxdigital.support
mysql_root_pw: secure-password
# For a multi server setup use an actual interface
# mysql_host_ip: "{{ hostvars[mysql_host]['ansible_eth1']['ipv4']['address'] }}"
# For a one box wonder use localhost
mysql_host_ip: "{{ hostvars[mysql_host]['ansible_lo']['ipv4']['address'] }}"

# PHP: php53u or php54 from IUS
php_version: php53u

# Webserver: nginx, apache
webserver: nginx
vhost_domain: www.raxdigital.support
vhost_user: raxdigital
vhost_user_uid: 855
document_root: /var/www/vhosts/{{ vhost_domain }}/httpdocs

# Redis
redis_host: redis.raxdigital.support
redis_host_ip: "{{ hostvars[redis_host]['ansible_eth0']['ipv4']['address'] }}"
redis_port: 6379

# Memcache - Coming Soon
# memcache_host: memcached.raxdigital.support
# memcache_host_ip: "{{ hostvars[memcache_host]['ansible_eth0']['ipv4']['address'] }}"
# memcache_object_port: 11211
# memcache_session_port: 11212
